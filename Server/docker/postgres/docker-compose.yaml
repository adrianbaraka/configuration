services:
  db:
    image: postgres:15.15-trixie
    restart: unless-stopped
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: mydatabase
    ports:
      - "5432:5432"
    volumes:
      - /home/abc/Downloads/temp/volumes/postgres:/var/lib/postgresql/data 
